name: shared
description: "Shared utilities, constants, and mocks for the Roomate app"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.10.0"

dependencies:
  data:
    path: ../data/
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  freezed_annotation: ^3.1.0
  json_serializable: ^6.8.0
  json_annotation: ^4.11.0

dev_dependencies:
  flutter_lints: ^6.0.0
  build_runner: ^2.13.1
