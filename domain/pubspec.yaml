name: domain
description: "Domain layer for Roomate"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.10.0"

dependencies:
  dartz: ^0.10.1
  freezed_annotation: ^3.1.0
  freezed: ^3.2.5
  shared:
    path: ../shared
dev_dependencies:
  flutter_lints: ^6.0.0
  build_runner: ^2.13.1
