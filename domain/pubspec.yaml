name: domain
description: "Domain layer for Roomate"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.10.0"

dependencies:
  freezed_annotation: ^3.1.0
  shared:
    path: ../shared
dev_dependencies:
  flutter_lints: ^6.0.0
